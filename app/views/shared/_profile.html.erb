<a href="#" class="dropdown-toggle" data-toggle="dropdown">
  <% if current_user.image_url.present? %>
    <p><%= image_tag(@user.image_url.to_s) %>  <%=@user.name%><b class="caret">
    </b></p> 
  <% else %>
    <p><%= image_tag("default.png") %>  <%=@user.name%><b class="caret">
    </b></p> 
    <% end %>
  </a>